{
  "private": true,
  "description": "Build tools for LibrePOS static assets",
  "scripts": {
    "minify:css": "node build.mjs css",
    "minify:js": "node build.mjs js",
    "minify": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
